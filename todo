----------- v1.1 -----------

переписать newEvent и waitNewEvent, чтобы принимали PartialFunction

сделать State immutable

переписать вызов server_socket.accept внутри future вместо актора.

сделать возможность мультипоточной работы с библиотекой: handlers, registerHandler(actorRef)

синхронизированные методы sendSync, sendToClientSync, sendToAllClientsSync

сделать disconnect синхронизированным (чтобы блокировался пока дисконнект не отработает). В идеале нужно добиться
корректного завершение сервера или клиента перед выходом из программы. Мб еще реализовать рестарт.

дописать в ридми про выбор доступного порта!

дописать в ридми про isconnected

дописать в сервере метод, возвращающий количество подключенных клиентов и мб инфу по ним.

----------- v1.0 -----------